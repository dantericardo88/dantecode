# Whitelist approach: exclude everything, then re-include what's needed.
# (tsup bundles all workspace deps into dist/extension.js — no node_modules needed)
**

!dist/
!dist/extension.js
!assets/
!assets/**
!package.json
!README.md
!LICENSE.txt
!CHANGELOG.md
