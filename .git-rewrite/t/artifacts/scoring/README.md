# DanteCode Scoring Evidence

This directory holds manual and external evidence that cannot be inferred from source code alone.

Populate these manifests with real measurements before claiming 9+ distribution scores:

- `docs-time-to-value.json`
- `external-users.json`
- `skill-ecosystem.json`
- `install-success.json`

The automated score report is written to `current-score-report.json`.
